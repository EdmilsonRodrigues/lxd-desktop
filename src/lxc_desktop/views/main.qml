import QtQuick;
import QtQuick.Controls;

ApplicationWindow {
    id: mainWindow;
    visible: true;
    width: Screen.desktopAvailableWidth * 1; 
    height: Screen.desktopAvailableHeight * 1;
    color: 'white'
}
